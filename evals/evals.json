{
  "skill_name": "spec-debate",
  "notes": "Functional end-to-end dogfood (not baseline-vs-skill): without the skill the agent wouldn't call a reviewer, vet, or persist state, so there is no sensible 'no-skill' baseline. These nine target DISTINCT critical paths with no overlap: (1) iterable round 1, (2) multi-round continuation, (3) prompt-only consult, (4) code change-spec, (5) privacy-mode, (6) idea/claim → position-spec iterable, (7) reviewer routing by model name (opencode), (8) no-name default + fallback chain, (9) worker-not-reviewer must-NOT-fire. Triggering and effort-parsing are folded as expectations inside evals 1/3/4; context-resolution is folded into eval 2 (it resolves the in-progress spec from the conversation). Evals are LIVE runs needing the respective CLIs authenticated (evals 1-6: codex; eval 7: opencode; eval 8: whichever harnesses are installed) — they cost money and edit files, so they are a manual pre-release gate. Setup: eval 1 needs a short throwaway spec at the referenced path; eval 2 must run IMMEDIATELY AFTER eval 1 in the same conversation/workdir (it depends on eval 1's spec + state); evals 4-5 each need a tiny throwaway code file at the referenced path; evals 6-9 need no pre-seeded file.",
  "evals": [
    {
      "id": 1,
      "prompt": "I've just finished the spec for an internal URL-shortener service (/tmp/spec-debate-test/url-shortener-spec.md). Run it through a debate with codex and improve it.",
      "expected_output": "Round 1: Claude and codex each independently propose improvements (the Codex prompt does NOT contain Claude's current-round proposal list); Claude merges both lists with veto (verifying against the actual file, not blindly), applies accepted ones via Edit, writes a skimmable report (accepted/partial/rejected with one-line reasons and source codex|own), and creates .url-shortener-spec.md.debate-state.json with round 1 recorded. Effort defaults to high (no effort word in the prompt).",
      "files": ["/tmp/spec-debate-test/url-shortener-spec.md"],
      "expectations": [
        "A Codex critique ran via scripts/run_codex_critique.sh (exactly one codex exec at a time)",
        "The Codex prompt file did NOT contain Claude's own current-round proposal list (independence preserved)",
        "The report and state include `own`-sourced findings that went through the same accept/partial/reject vetting as Codex's (rejected own proposals are reported whenever they occur)",
        "Every finding has a one-line reason; effort defaulted to high (no effort directive in the prompt)",
        "Accepted/partial edits were applied to the spec file via Edit, in the spec's voice",
        ".url-shortener-spec.md.debate-state.json was created beside the spec and parses as a single valid JSON document with round 1 recorded"
      ]
    },
    {
      "id": 2,
      "prompt": "Do one more iteration of improvements on the spec.",
      "expected_output": "Run immediately after eval 1 in the same conversation. The skill resolves 'the spec' from conversation context (no path given), reads the existing .url-shortener-spec.md.debate-state.json, sets round=2, passes settled (rejected/partial) verdicts to codex so they are not re-raised, gathers fresh proposals from both models, merges with veto, applies accepted edits, reports with convergence status, and appends round 2 to the state file.",
      "files": ["/tmp/spec-debate-test/url-shortener-spec.md"],
      "expectations": [
        "The spec was resolved from the conversation context (the prompt gives no path), not guessed across the filesystem",
        "The existing debate-state file was loaded and the new round numbered 2",
        "Settled rejected/partial verdicts from round 1 were passed to Codex as not-to-re-raise",
        "Fresh proposals were gathered from both models and merged with veto",
        "The report includes a convergence assessment, and round 2 was appended with the state still parsing as one valid JSON document"
      ]
    },
    {
      "id": 3,
      "prompt": "I'm torn between Postgres LISTEN/NOTIFY and a small Redis stream for the job queue in our internal tool (~5 jobs/sec, one Postgres already in the stack). Get codex's take on which fits better.",
      "expected_output": "The skill triggers on the explicit second-opinion request even though the words 'debate' and 'spec' do not appear. It picks the prompt-only path (a bounded advisory question), runs exactly one Codex pass via the helper, and reports inline: Codex's position, Claude's own vetted take (Step 4 lens), the prompt-file path, and a one-line 'sent to Codex' note. It creates no debate-state file and runs no rounds (offering to escalate only if the question turns out under-scoped mid-pass).",
      "files": [],
      "expectations": [
        "Triggered on a casual second-opinion request with neither 'debate' nor 'spec' in the prompt",
        "Explicitly chose and stated prompt-only mode",
        "Exactly one codex pass via scripts/run_codex_critique.sh; no rounds were run",
        "No new subject-specific .debate-state.json file was created for this question",
        "The inline report gives Codex's position, Claude's own vetted take, the prompt-file path, and a one-line 'sent to Codex' note"
      ]
    },
    {
      "id": 4,
      "prompt": "think it through with codex at maximum reasoning depth before I refactor the rate-limiter in /tmp/spec-debate-test4/ratelimit.py — don't touch the code yet.",
      "expected_output": "The skill triggers (no 'debate'/'spec' wording), parses 'maximum reasoning depth' as xhigh, and recognizes CODE mode: it drafts a change-spec to a file from the task + the code, runs one debate round at xhigh via the helper, and merges with veto. The source code is NOT edited during the debate; the change-spec carries acceptance checks/tests and treats applying-to-code as a separate post-debate step.",
      "files": ["/tmp/spec-debate-test4/ratelimit.py"],
      "expectations": [
        "Triggered on 'think it through with codex' (no 'debate'/'spec' wording)",
        "'maximum reasoning depth' was parsed as effort xhigh and passed to the helper",
        "CODE mode recognized: a change-spec file was drafted and the source code (ratelimit.py) was NOT edited during the debate",
        "A debate round completed with the merge-with-veto report",
        "The change-spec includes acceptance checks/tests and treats applying-to-code as a separate post-debate step"
      ]
    },
    {
      "id": 5,
      "prompt": "Debate this billing module's redesign with codex, but privacy mode — don't send the actual code, just an abstracted summary. The goal is to make invoice creation idempotent so webhook retries can't double-charge. Code is in /tmp/spec-debate-test5/billing.py.",
      "expected_output": "The skill triggers and honors privacy-mode: Codex receives only an approved abstracted summary built around the stated goal (idempotent invoice creation / no double-charge on webhook retries), never the raw code; the codex prompt file contains no verbatim source or secrets. Claude marks Codex's confidence as limited (it judged from a summary) and still produces a vetted report — or declines the Codex pass with a reason if the question cannot be judged without the code.",
      "files": ["/tmp/spec-debate-test5/billing.py"],
      "expectations": [
        "Privacy-mode was recognized from 'privacy mode / don't send the code'",
        "The codex prompt file contained only an abstracted summary (built around the idempotency goal) — no verbatim source code from billing.py",
        "Codex's confidence was marked as limited (judged from a summary, not the code)",
        "A vetted report was produced, or the Codex pass was declined with a clear reason if unjudgeable from the summary"
      ]
    },
    {
      "id": 6,
      "prompt": "I want to pin down a claim before I write it up: 'for our internal tools we should default to a modular monolith, not microservices, until we pass ~4 teams.' Stress-test it with codex over two rounds and tighten the argument.",
      "expected_output": "No existing spec and no code file: the skill recognizes an IDEA/CLAIM/THESIS input, drafts a short position-spec in the working dir, then uses that artifact for two iterative debate rounds with Codex and merge-with-veto. It creates/updates .debate-state.json as one valid JSON document. It does not ask for a missing spec, does not enter CODE mode, and does not stay prompt-only.",
      "files": [],
      "expectations": [
        "Triggered on the explicit second-model hand-off: 'stress-test it with codex'",
        "Recognized IDEA/CLAIM/THESIS with no existing spec file and no source file",
        "Drafted a short position-spec artifact containing claim-as-goal, assumptions, constraints, strongest counter-arguments, conditions/limits, and open questions",
        "Ran exactly two iterative rounds via scripts/run_codex_critique.sh using that position-spec as the debated artifact",
        "Applied merge-with-veto to tighten the position-spec across rounds",
        ".debate-state.json was created/updated in the working dir and parses as one valid JSON document",
        "Did not enter CODE mode and did not stay prompt-only"
      ]
    },
    {
      "id": 7,
      "prompt": "Ask kimi whether this plan holds up: cache external-API responses in process memory with a 5-minute TTL, key = URL.",
      "expected_output": "The skill triggers on a named reviewer model (kimi) and routes to the opencode harness: Step 0 resolves the latest kimi version (discovered via `opencode models`, matched by model-family basename), the critique runs via scripts/run_critique.sh opencode with the prompt fed over stdin, and the report names the reviewer and its provider/gateway. No codex call is made.",
      "files": [],
      "expectations": [
        "Triggered on 'ask kimi' — a reviewer model named by name, no harness word in the prompt",
        "Resolved kimi -> opencode harness with the LATEST kimi version (family matched by basename glob; an unresolvable family errors instead of silently substituting)",
        "The critique ran via scripts/run_critique.sh opencode with the prompt fed via stdin (never argv)",
        "No codex invocation occurred (single named reviewer respected)",
        "The report names the reviewer model and its actual provider"
      ]
    },
    {
      "id": 8,
      "prompt": "Get a second opinion on this plan from someone: store user sessions only in a client-side JWT, no server-side store.",
      "expected_output": "No reviewer named: Step 0 applies the default chain codex → opencode → claude, preflighting the resolved harness (CLI present + auth/config) and falling to the next only when one is missing — never silently substituting a *named* harness. The debate runs on the first usable harness and the report states which reviewer/provider was used.",
      "files": [],
      "expectations": [
        "Triggered on an explicit external-opinion request with no model or harness named",
        "The default chain codex → opencode → claude was applied in order, preflighting the resolved harness (command -v + auth/config)",
        "Exactly one reviewer harness ran the critique via scripts/run_critique.sh",
        "The report names which reviewer/provider was actually used (so a fallback is visible to the user)"
      ]
    },
    {
      "id": 9,
      "prompt": "Rewrite this function with kimi: def median(nums): nums.sort(); return nums[len(nums) // 2]",
      "expected_output": "The skill does NOT trigger: kimi is named as the WORKER ('do the task with model X'), not as a reviewer of an artifact. The agent handles or declines the rewrite itself; no spec-debate invocation, no run_critique.sh call, no debate-state file.",
      "files": [],
      "expectations": [
        "The skill did NOT fire (no Skill invocation, no run_critique.sh / run_codex_critique.sh call)",
        "No .debate-state.json file was created",
        "The agent treated the request as a coding task (its own work), not as a review hand-off"
      ]
    }
  ]
}
